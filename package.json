{
  "name": "maqlab",
  "version": "1.0.0",
  "private": true,
  "description": "MAQLAB (مقلب) — bilingual (ar/en) multiplayer party game",
  "main": "server.js",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.23.0",
    "qrcode": "^1.5.4",
    "socket.io": "^4.8.1"
  }
}
